name: DevSecOps Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  security-pipeline:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # ============================================
      # GITLEAKS - SIN CONTINUE-ON-ERROR
      # ============================================
      - name: Gitleaks
        uses: zricethezav/gitleaks-action@v1.6.0
        # ❌ QUITADO: continue-on-error: true

      # ============================================
      # SNYK - SIN || true
      # ============================================
      - name: Install system dependencies and Snyk
        run: |
          sudo apt-get update
          sudo apt-get install -y libjpeg-dev zlib1g-dev
          pip install --no-cache-dir -r requirements.txt
          npm install -g snyk
          snyk --version

      - name: Snyk Test
        run: |
          snyk test --severity-threshold=high  # ❌ QUITADO: || true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Install snyk-to-html
        if: always()
        run: npm install -g snyk-to-html@1.4.0

      - name: Generate Snyk HTML Report
        if: always()
        run: |
          snyk test --severity-threshold=high --json > snyk-report.json || true
          snyk-to-html -i snyk-report.json -o snyk-report.html || true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # ============================================
      # SONARCLOUD - SIN CONTINUE-ON-ERROR
      # ============================================
      - name: SonarCloud Scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.organization=negrete-8
            -Dsonar.projectKey=negrete-8_pygoat
            -Dsonar.sources=.
        # ❌ QUITADO: continue-on-error: true

      # ============================================
      # ZAP - SIN CONTINUE-ON-ERROR
      # ============================================
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install ZAP API
        run: pip install python-owasp-zap-v2.4

      - name: Build and Run Flask App
        run: |
          cat > Dockerfile << 'EOF'
          FROM python:3.9-slim
          WORKDIR /app
          COPY requirements.txt .
          RUN pip install --no-cache-dir -r requirements.txt
          COPY . .
          EXPOSE 5000
          CMD ["python", "app.py"]
          EOF
          
          docker build -t flask-vulnerable .
          docker run -d --name flask-app --network host flask-vulnerable
          echo "⏳ Esperando 30 segundos..."
          sleep 30

      - name: Run ZAP
        run: |
          docker run -d --name zap --network host -v $(pwd):/zap/wrk:rw \
            zaproxy/zap-stable \
            zap.sh -daemon -host 0.0.0.0 -port 8080 \
            -config api.key=${{ secrets.ZAP_API_KEY }}
          echo "⏳ Esperando 30 segundos..."
          sleep 30

      - name: ZAP Scan
        run: python zap_scan.py
        env:
          ZAP_API_KEY: ${{ secrets.ZAP_API_KEY }}
        # ❌ QUITADO: continue-on-error: true

      # ============================================
      # ARTIFACTS - SIEMPRE SUBIR REPORTES
      # ============================================
      - name: Upload Snyk Report
        uses: actions/upload-artifact@v4
        if: always()  # ✅ Esto SÍ se queda para ver los reportes aunque falle
        with:
          name: snyk-report
          path: snyk-report.html
          if-no-files-found: warn

      - name: Upload ZAP Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-report
          path: zap-report.html
          if-no-files-found: warn

      # ============================================
      # BUILD DOCKER - SOLO SI TODO ÉXITO
      # ============================================
      - name: Build Docker image
        if: success()
        run: |
          docker tag flask-vulnerable pygoat:final || docker build -t pygoat:final .
          echo "✅ Imagen Docker construida: pygoat:final"
